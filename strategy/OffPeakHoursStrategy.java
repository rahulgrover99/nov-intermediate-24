package com.example.splitwise.demo.strategy;

public class OffPeakHoursStrategy implements SlotFinderStrategy{
    @Override
    public Slot findSlot(Vehicle vehicle) {

        // nearest spot possible.

        return new Slot();
    }
}
